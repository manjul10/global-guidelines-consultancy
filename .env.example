# Database Connection (Default: local SQLite file for zero-config dev; swap for PostgreSQL in prod)
DATABASE_URL="file:./dev.db"

# For PostgreSQL (Neon / Supabase / Railway / Docker):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/consultancy_db?schema=public"

# NextAuth Configuration
NEXTAUTH_SECRET="development-secret-superpowers-antigravity-2026-key"
NEXTAUTH_URL="http://localhost:3000"

# Storage Configuration
UPLOAD_DIR="./public/uploads"
